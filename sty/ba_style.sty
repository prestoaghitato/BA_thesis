% @author Marcel Ruland (2018)

\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{ba_style}

\usepackage{fontenc}  % Open Type fonts
\usepackage{ttcommons}  % use TT commons for sf
\usepackage{ttjenevers}  % use TT Jenevers for rm
\setmonofont[Scale=MatchLowercase]{Envy Code R}  % use Envy Code R for tt
\usepackage[english]{babel}  % English hyphenation patterns
\usepackage[defaultlines=2,all]{nowidow}  % prevent widow and orphan lines
\usepackage{graphicx}  % required to insert images
\usepackage[usenames,dvipsnames]{xcolor}  % Required for custom colors
\usepackage{amsmath,amssymb}  % Better maths support & more symbols
\usepackage{microtype}  % improved typography

% change to onehalfspacing later!
%\usepackage[onehalfspacing]{setspace}  % improved linespacing
\usepackage[singlespacing]{setspace}  % improved linespacing

\usepackage{lipsum}  % blind text
\usepackage{booktabs}  % improved tables
\usepackage{array}  % >{} syntax to format table columns
\usepackage{enumitem}  % enumerate environment, custom list labels
\usepackage{localextra}  % provides colour definitions and hyphenation for linguistic terms, names, etc.
\usepackage{harvard}  % use Harvard citation style
	\bibliographystyle{newharvard}
	\citationstyle{dcu}
\usepackage{tikz}
%\usepackage{scrlayer-scrpage} % uncomment for custom header (chapter 5 of manual)
%	\lohead{loh}
%	\cohead{coh}
%	\rohead{roh}
%	\lofoot*{lof}
%	\rofoot*{rof}
%	\cofoot*{cof}
%	\pagestyle{scrheadings}
%**************************************************



%*******************KOMA-Options*******************
% \KOMAoptions{parskip=half,headsepline,footsepline} % uncomment this one
%**************************************************



%**************Customise Font Formats**************
\addtokomafont{author}{\sffamily\addfontfeature{Style=Alternate}}  % amend author
% Elements on page 60 of (German) KOMA manual

%% Typographic and General Issues:
%en dash or em dash? See \dash command below.
%Describe figures in caption even though main text contains a description?
% Bibliography: Capitalise only very first word, words after colons, and proper nouns.
%**************************************************



%****************** New Commands ******************
%% shortcuts
\newcommand{\fpm}{Frequent Pattern Mining}  % shortcut

%% semantic commands
\newcommand{\dash}{---}  % typeset dashes
\newcommand{\pnum}[1]{p.~#1}  % page numbers in citations

\newcommand{\code}[1]{\texttt{#1}}  % code
\newcommand{\fpmlabel}[1]{\(\mathcal{#1}\)}  % typeset label
\newcommand{\fpmset}[1]{\(\{\)\fpmlabel{#1}\(\}\)}  % typeset label sets
\newcommand{\fpmrule}[2]{\fpmset{#1} \(\rightarrow\) \fpmset{#2}}  % typeset rules

%% section commands
% chmethods
\newcommand{\imprecisiondelay}{XXXms}  % maximum delay accounting for coders' imprecision
\newcommand{\reactiontime}{XXXms}  % minimum delay for interpersonal sequences.
%**************************************************



%****************** New Colours *******************
%\definecolor{white}{HTML}{FFFFFF}
%**************************************************






